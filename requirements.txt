# Core dependencies
# (No runtime dependencies required for basic Morse translator)

# Testing and development
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: Code style & linting (good practice for growing projects)
flake8>=7.0.0